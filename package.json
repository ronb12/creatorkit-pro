{
  "name": "creatorkit-pro",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "node --check app.js && node --check api/bootstrap.js && node --check api/ideas.js && node --check api/campaigns.js && node --check api/deliverables.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2"
  }
}
